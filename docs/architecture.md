# Architecture

What problem does this platform solve?
The platform allows users to run heavy galaxy and N-body simulations in the cloud without needing to set up scientific libraries on their own machine. Rather than editing Python scripts by hand, users define their simulation settings through a web form, the computation runs asynchronously, and once it's done they get back performance metrics, plots, or visual results.
Who is the user?
